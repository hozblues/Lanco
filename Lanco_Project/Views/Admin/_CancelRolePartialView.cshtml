@model Lanco_Project.Models.Roles

<div class="modal fade" id="CancelRoleModal-@Model.RoleID" tabindex="-1" aria-labelledby="CancelRoleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="CancelRoleModalLabel">Desactivar Usuario</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            @using (Html.BeginForm("CancelRole", "Admin", FormMethod.Post))
            {
                <div class="modal-body">
                    <div class="mb-1">
                        @Html.EditorFor(model => model.RoleID, new { htmlAttributes = new { @class = "form-control", @type = "hidden" } })
                        <label class="form-label">¿Estas seguro en desactivar el Rol?</label>
                        <br />
                        <label>
                            <strong>
                                @Html.DisplayFor(model => model.RoleName)
                            </strong>
                        </label>
                    </div>
                </div>
                <div class="modal-footer">
                    <button id="btnCloseCancelRole" type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                    <button id="btnCancelRole" type="submit" class="btn btn-primary">Desactivar</button>
                </div>
            }
        </div>
    </div>
</div>